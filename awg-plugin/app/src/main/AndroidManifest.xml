<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.telegram.awg.plugin"
    android:sharedUserId="org.telegram">
    <!--
    IMPORTANT: android:sharedUserId="org.telegram" MUST match the sharedUserId
    in AyuGram's AndroidManifest.xml. This allows direct SharedPreferences access
    between the two apps (same Linux UID = same file permissions).

    Both apps must be signed with the same certificate for sharedUserId to work.
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- NO android.permission.BIND_VPN_SERVICE — we don't use system VPN! -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <!-- Main settings activity -->
        <activity
            android:name=".AWGPluginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background service (NO VPN service!) -->
        <service
            android:name=".AWGPluginService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot receiver — restart service after reboot -->
        <receiver
            android:name=".AWGBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ═══════════════════════════════════════════════════════════
             MODIFICATION NEEDED IN AYUGRAM's AndroidManifest.xml:
             Add the following receiver declaration:

        <receiver
            android:name="org.telegram.awg.plugin.AWGProxyReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="org.telegram.awg.action.SET_PROXY" />
                <action android:name="org.telegram.awg.action.CLEAR_PROXY" />
                <action android:name="org.telegram.awg.action.STATUS" />
            </intent-filter>
        </receiver>
             ═══════════════════════════════════════════════════════════ -->

    </application>
</manifest>
